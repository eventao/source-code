<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= title %></title>
</head>

<body>
<p>Hi <%= name %></p>
<h1><%= title %></h1>
<ul>
    <% for(var i=0; i<supplies.length; i++) {%>
    <li><%= link_to(supplies[i], 'img/'+supplies[i]) %></li>
    <% } %>
</ul>
</body>
</html>