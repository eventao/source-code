//
   Created by 涛 on 2017/3/4.

doctype html
html(lang="en")
    head
        title #{title}
        script(type='text/javascript',src='/jade/scripts/index.js')
    body
        h1 Pug - node template engine
        #container.col
            if youAreUsingPug
                p You are amazing
            else
                p Get on it!
            p.
                Pug is a terse and simple templating language with a
                strong focus on performance and powerful features.
        each user,index in users
            if user.role == 'admin'
                p(style='background-color:black;color:white') #{user.name} 是系统管理员
            else
                p #{user.name} 是普通用户

